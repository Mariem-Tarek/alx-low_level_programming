#include <stdio.h>
#include "main.h"

/**
 * add - function prints
 *
 * @a : parameter to be checked
 * @b : parameter to be cheaked
 *
 * Return: 0 always (Successd)
 */
int add(int a, int b)
{
int result;
result = a + b;
printf("%d", result);
return (0);
}
