#include "main.h"

/**
 *  _abs  - function prints
 *
 * @x: parameter that taken to be checked
 *
 * Return: Always 0 (Success)
 */
int _abs(int x)
{
if (x >= 0)
{
	x = x;
}
else
{
	x = x * (-1);
}
	return (x);
}
